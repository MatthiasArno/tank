# Tank Monitoring System - Configuration
# Copy this file to .env and fill in your values
# Then install the package on the server and copy .env there (do not include .env in the package)

# MQTT Broker Configuration
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=

# Telegram Bot Configuration
# Token from @BotFather (https://t.me/BotFather)
TELEGRAM_BOT_TOKEN=your-bot-token-here

# Your Telegram User ID (from @userinfobot)
TELEGRAM_MY_ID=your-user-id-here

# Database
# Default: ../db/tank_data.db (relative to rpi/src)
# DATABASE_PATH=/custom/path/to/tank_data.db

# Web Server (Dashboard)
WEB_HOST=0.0.0.0
WEB_PORT=8000

# Logging
LOG_LEVEL=INFO

# Regression coefficients for fuel consumption per in/out temperature difference.
# Use statistic_db.py go generate proper coefficients based on historical data.
# Make sure the date comprise wide range of temperatures and enough data points.
REGRESSION_K=-0.921543
REGRESSION_C=18.430868

# Wheather forecase sources
# brightsky = BrightSky.dev (standard, ca. 10-14 days)
# openmeteo = Open-Meteo (alternative, ca. 16 days)
FORECAST_SOURCE=openmeteo

# Coordinates for your location, adapt using google maps to fetch proper wheather forecasts
LOCATION_LAT=50.000
LOCATION_LON=8.000
# ZIP is just any name to indicate the location of the device
LOCATION_ZIP="Postalcode"
